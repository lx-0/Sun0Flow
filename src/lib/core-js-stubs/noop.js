// Generic stub for removed core-js-pure features/* paths.
// All targeted environments (Node >= 18, evergreen browsers) have native
// implementations. Returning undefined causes callers to fall back to the
// built-in, which is the correct behaviour.
module.exports = undefined;
